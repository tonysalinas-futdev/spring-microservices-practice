server:
  port: 8023

spring:
  application:
    name: driver-microservice
  cloud:
    config:
      username: root
      password: s3cr3t
  config:
    import: optional:configserver:http://localhost:8099,optional:file:./driver-microservice/.env[.properties],optional:file:./.env[.properties]

