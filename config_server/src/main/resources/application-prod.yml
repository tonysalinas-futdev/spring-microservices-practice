spring:
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI}
          search-paths: config_data
          default-label: main

  security:
    user:
      name: root
      password: s3cr3t

