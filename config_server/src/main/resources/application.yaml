spring:
  application:
    name: config_server

  config:
    import: optional:file:.../../.env[.properties],optional:file:./.env[.properties]

  profiles:
    active: ${ACTIVE_PROFILE}

server:
  port: 8099